package CreationalPatterns.Prototype;

public interface LevelPrototype {

    // function that basically instantiate an object of the same
    // concrete class, assign the values and return the cloned object
    public LevelPrototype cloneLevel();


}
